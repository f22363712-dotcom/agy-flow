我现在遇到一个问题：我经常使用 AI，但目前只购买了 deepseek 的 Api，另外订阅了 ChatGPT/codex 和 gemini/antigravity
，但没有它们的 api。我想要搭建一个跨平台/Agent 的框架，使得我在进行任意一项任务时，无论平台是 VSCode 中的 Claude 或 Codex 扩展，还是在 Codex 桌面端以及 Antigravity 中，都能根据任务自动调用对应的 AI Agent，充分发挥多 AI 的能力，提高开发效率和质量，降低 单一 api 账单费用。目前是否存在类似方案和功能？请你帮我调研一下











已搜索网页（2026 multi agent AI coding orchestration framework Codex Claude VSCode Gemini MCP OpenRouter LiteLLM OpenHands official）

有，而且你这个需求已经对应到一个很明确的技术方向了：Multi-Agent Orchestration / Agent Router / LLM Gateway / Agent Harness。
但先给你一个关键结论：
目前没有一个成熟的开箱即用方案，能够把 ChatGPT Plus、Codex 订阅、Gemini 订阅、Antigravity 订阅、Claude Code、VSCode 扩展等“消费级订阅额度”统一抽出来，由中央调度器任意调用。

原因是：订阅权限 ≠ API 权限 ≠ Agent Runtime 权限。
不过，已经存在大量组件，可以搭出非常接近你设想的系统。并且考虑到你目前是学生、已经订阅 ChatGPT 和 Gemini、只有 DeepSeek API，我认为你完全没必要继续购买一堆 API。
更适合你的方案是：
Agent Runtime + Agent Protocol + MCP + LLM Gateway + 自定义 Router

我调研了目前的项目和技术路线。下面我直接结合你的实际情况讲。
一、先定义一下你真正想做的东西
你想要的其实不是普通的“多模型调用”。
你想实现的是：
                        ┌────────────────────┐
                        │     User Task      │
                        │  VSCode/Desktop/CLI│
                        └─────────┬──────────┘
                                  │
                                  ▼
                       ┌──────────────────────┐
                       │   Task Router / Brain │
                       │                      │
                       │ 任务分类              │
                       │ 成本判断              │
                       │ Agent选择             │
                       │ 并行/串行调度          │
                       └──────────┬───────────┘
                                  │
              ┌───────────────────┼───────────────────┐
              ▼                   ▼                   ▼

        Codex Agent         Gemini Agent        DeepSeek Agent

        Coding              Research            Cheap Worker
        Debug               Search              Code Review
        Refactor            Long Context        Batch Tasks

              │                   │                   │
              └───────────────────┼───────────────────┘
                                  ▼

                       ┌──────────────────────┐
                       │ Shared Agent Layer   │
                       │                      │
                       │ MCP Servers          │
                       │ Skills               │
                       │ Memory               │
                       │ Git Workspace        │
                       │ Task State           │
                       └──────────────────────┘
例如你输入：
“分析这个项目，找出性能问题并修复。”

系统自动拆成：
DeepSeek
↓
扫描代码
生成项目结构摘要
定位疑似性能热点

Gemini
↓
阅读大型代码库
搜索相关文档
分析依赖与架构

Codex
↓
修改代码
运行测试
修复错误

DeepSeek
↓
Review diff

Codex
↓
最终修复
而不是：
用户
↓
Claude Code

用户
↓
Codex

用户
↓
Gemini
这两者是完全不同的生产力范式。
二、目前有哪些方案最接近你的需求？
我认为有 5 类项目值得关注。
1. LiteLLM：最适合做你的“模型网关”
LiteLLM 官方文档
LiteLLM 本质上是：
Unified LLM Gateway
统一：
OpenAI
Anthropic
Gemini
DeepSeek
OpenRouter
Ollama
Azure
...
你可以让所有 Agent 调用：
localhost:4000/v1
然后 LiteLLM 决定：
请求
 ↓
DeepSeek

失败
 ↓
Gemini

超预算
 ↓
Local Model
它已经支持：
Routing
Load Balancing
Budget
Rate Limit
Fallback
Spend Tracking
Virtual Keys
尤其是 Budget Routing 和 Budget Fallback，非常符合你“降低单一 API 账单”的目标。LiteLLM
但它有一个问题：
LiteLLM 调度的是 Model，不是 Agent。

也就是说：
DeepSeek API
Gemini API
OpenAI API
可以。
但是：
Codex CLI
Claude Code
Gemini CLI
不能直接作为 LiteLLM Provider 使用。
所以你还需要第二层。
2. OpenHands：目前最接近“Agent Runtime”的项目
OpenHands 官方文档
这是我认为你应该重点研究的项目。
OpenHands 已经逐渐从：
AI Coding Agent
发展成：
Agent Runtime
+
Agent SDK
+
Agent Orchestration Framework
它的 Software Agent SDK 可以定义 Agent、在本地运行，并扩展到大量 Agent 实例。OpenHands Docs
更关键的是，它已经支持 MCP：
Agent
 ↓
MCP

Git
Filesystem
Browser
Database
Search
Custom Tools
Agent 可以动态发现和使用 MCP 工具。OpenHands Docs
而 OpenHands 的文档索引里还明确出现了一个非常重要的能力：
ACP Agent 可以把任务委托给 Claude Code、Gemini CLI 等 ACP-compatible server，而不是直接调用 LLM。OpenHands Docs

这已经非常接近：
Orchestrator

↓ delegate

Claude Code
Gemini CLI
Other Agents
所以我认为：
OpenHands SDK 是你这个系统非常值得考虑的 Agent Runtime。

3. OpenRouter：最成熟的“模型市场 + Router”
OpenRouter 官方文档
OpenRouter 提供：
统一 API

↓

数百个模型
并支持：
Provider Routing

Auto Router

Fallback

Cost Optimization
它的 Provider Routing 可以根据可用性、延迟等因素选择 Provider；Auto Router 则可以限制候选模型范围，让路由器自动选择模型。OpenRouter
但你的情况比较特殊。
你已经有：
DeepSeek API

ChatGPT Subscription

Gemini Subscription
所以如果使用 OpenRouter：
你还是要付 API 钱
因此现阶段它不是你的最优解。
4. Sema Code：理念与你的设想高度接近
这是我调研过程中发现的一个非常值得关注的 2026 年项目。
Sema Code 的设计目标就是解决：
Coding Agent
被锁在

CLI
IDE
Web App

里面
的问题。
它提出：
Agent Engine
      ↓

VSCode

CLI

Telegram

Feishu

Other Client
共用同一个 Agent Core。
其架构包含：
Multi-Agent Scheduling

MCP

Skills

Plugins

Context Compression

Task Management

Permission Control
论文描述的 Sema Core 已经同时驱动 VSCode Extension 和多渠道消息网关。arXiv
这个项目的理念其实和你描述的目标非常接近：
Agent 与 UI 解耦。

我建议关注，但目前不建议把你的整个系统押在它上面，因为成熟度和生态还需要观察。
5. Agent Framework
还有：
LangGraph

CrewAI

AutoGen

OpenAI Agents SDK

Google ADK
这些可以实现：
Manager Agent

↓

Research Agent

Coding Agent

Review Agent

Testing Agent
但存在一个问题。
它们主要调度：
LLM API Agent
而你的核心需求是：
Claude Code

Codex

Gemini CLI / Antigravity

DeepSeek API
所以单纯使用 LangGraph/CrewAI 并不能解决问题。
三、你最大的技术难点其实不是 Router
真正困难的是：
如何调用 Codex / Gemini / Claude Code Agent
因为：
Codex Subscription
≠
OpenAI API

Gemini Subscription
≠
Gemini API

Claude Subscription
≠
Anthropic API
但存在一个可行方向：
CLI Agent
↓
Agent Protocol
↓
Orchestrator
目前主要协议包括：
MCP
Agent Client Protocol（ACP）
A2A
其中：
MCP

Agent → Tools
而：
ACP / A2A

Agent → Agent
这点非常重要。
你的系统不应该尝试：
把 Codex 当模型 API
而应该：
把 Codex 当 Agent Runtime
例如：
Agent Router

    │

    ├── Codex Adapter
    │
    │   └── Codex CLI
    │
    ├── Gemini Adapter
    │
    │   └── Gemini CLI
    │
    ├── Claude Adapter
    │
    │   └── Claude Code
    │
    └── LLM Adapter
        │
        └── LiteLLM
             │
             ├── DeepSeek
             ├── Ollama
             └── OpenRouter
这才是正确的系统边界。
四、我最推荐你的架构
如果让我按照你现在的资源给你设计，我会使用：
                  VSCode
                     │
                  Desktop
                     │
                    CLI
                     │
                     ▼

              Agent Gateway

                     │

          ┌──────────┴──────────┐

          │                     │

     Task Router           Task Manager

          │                     │

          └──────────┬──────────┘

                     ▼

              Agent Registry

      ┌──────────────┼──────────────┐

      ▼              ▼              ▼

 Codex Agent    Gemini Agent    LLM Agent

 Codex CLI      Gemini CLI      LiteLLM

                                   │

                         ┌─────────┼─────────┐

                         ▼         ▼         ▼

                     DeepSeek   Ollama   OpenRouter


                     Shared Layer

                         │

              ┌──────────┼──────────┐

              ▼          ▼          ▼

             MCP       Memory      Git

             Skills    Task DB     Workspace
技术栈：
Agent Runtime

OpenHands SDK
或者
自研 Python Agent Runtime


Agent Gateway

FastAPI


Model Gateway

LiteLLM


Agent Protocol

ACP
MCP


State

SQLite
→ 后期 PostgreSQL


Queue

Redis


Observability

Langfuse
五、Router 应该怎么设计？
不要一开始使用 LLM Router。
先使用：
Rule Router
例如：
if task.type == "code_edit":
    agent = "codex"

elif task.type == "large_context":
    agent = "gemini"

elif task.type == "cheap_analysis":
    agent = "deepseek"

elif task.type == "research":
    agent = "gemini"

elif task.type == "code_review":
    agents = [
        "deepseek",
        "codex"
    ]
复杂任务：
Task

↓

Planner

↓

Task DAG

↓

Agent Scheduler
例如：
Build Login System

↓

Planner

↓

Architecture
   ↓
Gemini

Implementation
   ↓
Codex

Test
   ↓
Codex

Review
   ↓
DeepSeek

Security Review
   ↓
Gemini

Final Fix
   ↓
Codex
以后再增加：
Router Model
例如用：
DeepSeek Chat
判断：
{
  "task": "debug React app",

  "agent": "codex",

  "reason": "code execution required",

  "fallback": "gemini",

  "budget": "medium"
}
六、这里有一个非常关键的现实问题
你的目标是：
降低 API 费用。

但我不建议你设计：
所有 Agent 都参与
例如：
Task

↓

Gemini
DeepSeek
Codex

↓

Voting
这会导致：
Token × 3
而且增加延迟。
更好的方式是：
Cheap Agent First

↓

Confidence Check

↓

Escalation
例如：
DeepSeek

↓

confidence > 0.8

直接完成
否则：
DeepSeek

↓

Codex
复杂任务：
DeepSeek Planner

↓

Codex Worker

↓

DeepSeek Reviewer
只有非常困难的问题：
Gemini / Codex

↓

Cross Review
LiteLLM 已经提供预算、Fallback、路由和 Spend Tracking 等基础设施，可以很好地承担模型层成本控制。LiteLLM
七、我认为你甚至可以把它做成一个项目
因为目前生态存在一个明显缺口：
MCP
解决
Agent → Tool

LiteLLM
解决
App → Model

LangGraph
解决
Agent Workflow

OpenHands
解决
Agent Runtime
但是：
VSCode
Codex Desktop
Claude Code
Gemini CLI
Antigravity

↓

统一 Agent Router
目前没有形成真正成熟的标准产品。
你可以做：
Universal Agent Router
比如：
AgentMesh
核心：
agentmesh daemon
配置：
agents:

  codex:
    type: acp
    command: codex

  gemini:
    type: acp
    command: gemini

  deepseek:
    type: llm
    provider: litellm

routing:

  coding:
    primary: codex
    fallback: deepseek

  research:
    primary: gemini

  cheap:
    primary: deepseek
然后：
VSCode Extension

Codex

Claude Code

CLI

Desktop App
全部连接：
AgentMesh
这会是一个挺有意思的开源项目。
八、结合你的实际情况，我建议你按这个顺序做
你现在大三，而且已经有开发经验。
不要一开始做：
Universal Multi-Agent Platform
很容易变成一个巨大工程。
第一阶段，做：
Agent Router MVP
只支持：
DeepSeek API

Codex CLI

Gemini CLI
能力：
POST /task

↓

Task Classifier

↓

Agent Adapter

↓

Agent Execution

↓

Result
第二阶段：
Task DAG

Parallel Agents

Retry

Fallback

Budget

Logs
第三阶段：
MCP

Skills

Memory

Workspace Isolation
第四阶段：
VSCode Extension

Desktop UI
最终结论
有类似方案，但没有一个项目完整解决你的问题。
最接近的组合是：
OpenHands SDK
        +
LiteLLM
        +
MCP
        +
ACP
        +
Codex CLI
        +
Gemini CLI
        +
DeepSeek API
其中：
OpenHands
负责 Agent Runtime

LiteLLM
负责 Model Gateway

ACP
负责 Agent 调用

MCP
负责 Tool Sharing

你自己的 Router
负责：

Task → Best Agent
我尤其建议你先研究 OpenHands 和 LiteLLM。前者已经具备 Agent SDK、MCP、插件化能力，后者提供统一模型网关、预算控制、路由和故障转移；二者组合与目标架构的重合度最高。OpenHands Docs
另外，我觉得下一步最有价值的事情不是继续泛泛调研。
你可以把你目前电脑上的开发环境告诉我，例如：
Windows / macOS / Linux

是否安装 WSL

Codex Desktop 是否正常

Codex CLI 是否安装

Gemini CLI 是否安装

Antigravity 是否正常

VSCode 常用哪些 AI 插件

是否有服务器

是否使用 Docker
我可以直接根据你的环境，给你设计一个真正可以跑起来的 Multi-Agent Router MVP：
项目目录
+
架构图
+
docker-compose
+
LiteLLM 配置
+
Agent Adapter
+
Task Router
+
Codex/Gemini/DeepSeek 接入方式
+
VSCode 接入方案
而且我会特别帮你验证一个最关键的问题：如何尽可能合法、稳定地复用你已有的 Codex/Gemini 订阅能力，而不是为了这个系统重新购买 OpenAI/Gemini API。